{% extends '@EasyAdmin/default/edit.html.twig' %}

{% block main %}


    {% block entity_form %}
        {{ form_start(form) }}
            <div class="row">
                <div class="col-sm-4">
                    {{ form_row(form.name) }}
                </div>
                <div class="col-sm-4">
                    {{ form_row(form.slug) }}
                </div>
            </div>
        {{ form_end(form) }}


    {% endblock %}

{% endblock %}